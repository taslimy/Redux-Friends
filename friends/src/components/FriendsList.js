import React, { Fragment } from "react";
import { connect } from "react-redux";
import { getFriends } from "../actions";

const FriendsList = props => {
  return (
    <Fragment>
      <h1> Testing From FriendList :) </h1>
    </Fragment>
  );
};

export default FriendsList;
